dubbo:
  scan:
    base-packages: cn.springcloud.gray.dubbo.service
  protocols:
    dubbo:
      port: -1

feign:
  hystrix:
    enabled: true

server:
  port: 0

management:
  endpoints:
    web:
      exposure:
        include: dubborestmetadata